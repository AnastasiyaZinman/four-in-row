import styled from 'styled-components';
import { Title } from '../results/results.styles';

export const RegistrationContainer = styled.div`
	margin: 0 auto;
	width: 50%;
	display: flex;
	flex-direction: column;
`;

export const RegistrationTitle = styled(Title)`
	text-align: center;
`;
